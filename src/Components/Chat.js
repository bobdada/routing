import React from 'react';
import '../App.css';
import { Nav } from "../App"

function Chat({ path }) {

  return (
    <div className="Appa">
      <h1>{path}</h1>
      <h1>This is Chat</h1>
    </div>
  );
}

export default Chat;
